name: Unified CI Pipeline

on:
  push:
    branches: [main, master, develop]
    paths-ignore:
      - 'web/**'
      - 'docs/**'
      - 'assets/**'
      - '**.md'
      - '.github/**'
      - '!.github/workflows/**'
  # No `branches:` filter. Limiting this to main/master/develop meant a STACKED
  # pull request -- one opened against another feature branch, which is how a
  # multi-slice change is reviewed here -- ran no CI at all beyond the secret
  # scanner. The base branch's own green checks say nothing about the child's
  # diff, so the child merged unverified and only met CI after its parent
  # landed and it was retargeted. `push` stays limited to the long-lived
  # branches; the concurrency group below cancels superseded runs.
  pull_request:
    paths-ignore:
      - 'web/**'
      - 'docs/**'
      - 'assets/**'
      - '**.md'
      - '.github/**'
      - '!.github/workflows/**'
  workflow_call:
    inputs:
      skip_heavy_checks:
        description: 'Skip heavy checks (Dialyzer, full test suite)'
        type: boolean
        default: false
  workflow_dispatch:
    inputs:
      skip_heavy_checks:
        description: 'Skip heavy checks (Dialyzer, full test suite)'
        type: boolean
        default: false

# Cancel in-progress runs for the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read

env:
  MIX_ENV: test
  SKIP_TERMBOX2_TESTS: true
  TMPDIR: /tmp

jobs:
  # ========================================
  # Setup and Cache Preparation
  # ========================================
  # The toolchain pin lives in `mise.toml` and is read exactly once, here. The
  # versions setup-beam actually resolved become job outputs, so every other job
  # installs the same toolchain and every cache key is stamped with a real
  # resolved version rather than a literal someone has to remember to bump.
  setup:
    name: Setup & Cache
    runs-on: ubuntu-latest
    outputs:
      elixir-version: ${{ steps.beam.outputs.elixir-version }}
      otp-version: ${{ steps.beam.outputs.otp-version }}
      cache-key: ${{ steps.cache-key.outputs.key }}
      deps-cache-hit: ${{ steps.deps-cache.outputs.cache-hit }}
      should-run-heavy: ${{ steps.check-changes.outputs.should-run-heavy }}
    
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0  # Need full history for change detection
      
      - name: Detect changed files
        id: check-changes
        run: |
          # Check if we should run heavy checks
          if [ "${{ inputs.skip_heavy_checks }}" == "true" ]; then
            echo "should-run-heavy=false" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" == "push" ]; then
            echo "should-run-heavy=true" >> $GITHUB_OUTPUT
          else
            # For PRs, check if critical files changed
            CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD)
            if echo "$CHANGED_FILES" | grep -qE "(mix\.exs|mix\.lock|lib/.*\.ex$)"; then
              echo "should-run-heavy=true" >> $GITHUB_OUTPUT
            else
              echo "should-run-heavy=false" >> $GITHUB_OUTPUT
            fi
          fi
      
      # Ahead of the cache-key step, because the key is stamped with the
      # versions this step reports having installed.
      - name: Set up Elixir
        id: beam
        uses: erlef/setup-beam@v1
        with:
          version-file: mise.toml
          version-type: strict

      - name: Generate cache keys
        id: cache-key
        run: |
          # mix.exs hash precedes the mix.lock hash so a package @version bump
          # (which never touches mix.lock) busts the compiled _build cache. The
          # restore-keys fall back only within the same mix.exs, never across a
          # version change.
          echo "key=deps-${{ runner.os }}-${{ steps.beam.outputs.otp-version }}-${{ steps.beam.outputs.elixir-version }}-${{ hashFiles('**/mix.exs') }}-${{ hashFiles('**/mix.lock') }}" >> $GITHUB_OUTPUT

      - name: Cache dependencies
        id: deps-cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ steps.cache-key.outputs.key }}
          restore-keys: |
            deps-${{ runner.os }}-${{ steps.beam.outputs.otp-version }}-${{ steps.beam.outputs.elixir-version }}-${{ hashFiles('**/mix.exs') }}-
      
      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.clean --unused
          # `--check-locked`, with no fallback. The `|| mix deps.get` this
          # replaces made the check advisory: on a mismatch mix rewrote
          # mix.lock in place and the pipeline carried on green, so a green run
          # never established that the COMMITTED lock is what CI built. The
          # package job two hundred lines down already uses the hard form, and
          # its own comment explains why -- a rewriting fetch lets a package
          # build green here and be impossible to reproduce anywhere else.
          #
          # The fallback also had a cost every consumer of this cache paid. It
          # left deps/ holding versions the committed lock does not name while
          # the cache saved here is keyed on a hash of THAT lock, so a consumer
          # restoring the key died on "lock mismatch" for a dependency nobody
          # touched: on 2026-08-12 a sobelow-only dependabot bump red-ed master
          # via nx and phoenix.
          #
          # Pure upstream drift -- a transitive dep publishing a newer patch
          # after the lock was committed -- now fails here, loudly, naming the
          # dependency. That is a lockfile commit, not a mystery.
          mix deps.get --check-locked
          mix deps.compile

  # ========================================
  # Fast Checks (Always Run)
  # ========================================
  format:
    name: Format Check
    needs: setup
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}

      - name: Install hex and rebar
        run: |
          mix local.hex --force
          mix local.rebar --force

      # Self-heal a cache that disagrees with the checked-out lock; see the
      # note on setup's deps.get fallback. No-op when the cache is healthy.
      - name: Fetch dependencies
        run: mix deps.get

      - name: Check formatting
        run: mix format --check-formatted

      # The root check above reads the root .formatter.exs, whose `inputs` stop
      # at the repo root -- so it reaches a package file only when a caller
      # names one. This walks every package and runs its own gate. It compiles
      # nothing, which is why it lives here and not in `package-tests`; the
      # script says why at length, and asserts the two conditions that would
      # make that untrue.
      - name: Check package formatting
        run: bash scripts/check_package_formatting.sh

      - name: Check singleton allowlist
        run: bash scripts/check_singletons.sh

      # raxol_agent now has its own package-tests cell, but this guard stays
      # here: a schema_version bump that ships without a frozen golden journal
      # is unrecoverable, and this check is dependency-free, so it answers in
      # seconds without waiting on that cell's compile. Deliberately redundant.
      - name: Check journal golden fixtures
        run: elixir scripts/check_journal_goldens.exs

      # Prose rules over every tracked .md: unicode punctuation, ` -- ` as an
      # em-dash substitute, and broken relative links/anchors. These regressed
      # repeatedly while they lived only in a style note, and the manual sweeps
      # that cleaned docs/ never covered packages/.
      #
      # This workflow's paths-ignore covers 'docs/**' and '**.md', so this step
      # never runs on a documentation-only pull request. The prose half is
      # gated there instead by the `prose` job in `lockstep-deps.yml`, which
      # fires on Markdown and needs no compile. What is unique to this step is
      # the catalog-count half, which reads `Raxol.Playground.Catalog` and so
      # requires the compile.
      - name: Check documentation
        run: mix raxol.check_docs

  release-check:
    name: Package Release Check
    needs: setup
    runs-on: ubuntu-latest
    # The check shells out to `mix hex.build` once per package. System.cmd/3
    # cannot impose a timeout, so a build that hangs would otherwise sit here
    # until the six-hour default job limit.
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v7

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict

      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}

      - name: Install hex and rebar
        run: |
          mix local.hex --force
          mix local.rebar --force

      - name: Fetch dependencies
        run: mix deps.get

      - name: Dry-run Hex package builds
        run: mix raxol.release.check

  compile:
    name: Compilation Check
    needs: setup
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}

      - name: Install hex and rebar
        run: |
          mix local.hex --force
          mix local.rebar --force

      # Self-heal a cache that disagrees with the checked-out lock; see the
      # note on setup's deps.get fallback. No-op when the cache is healthy.
      - name: Fetch dependencies
        run: mix deps.get

      - name: Compile with warnings as errors
        run: mix compile --warnings-as-errors

  # ========================================
  # Parallel Test Execution
  # ========================================
  test-unit:
    name: Unit Tests
    needs: [setup, compile]
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}
      
      # Self-heal a cache that disagrees with the checked-out lock; see the
      # note on setup's deps.get fallback. No-op when the cache is healthy.
      - name: Fetch dependencies
        run: mix deps.get

      - name: Run unit tests
        run: |
          mix test --exclude integration --exclude property --exclude slow --exclude skip_on_ci
        env:
          MIX_TEST_PARTITION: 1

      - name: Upload coverage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-unit
          path: cover/unit.coverdata

      - name: Upload boot metrics
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: boot-metrics-unit
          path: _build/test/boot_metrics.json
          if-no-files-found: ignore

  # ========================================
  # Extracted Package Tests
  # ========================================
  # Packages not in the root umbrella's dependency set compile and test on
  # their own; the root jobs never see them.
  package-tests:
    name: Package Tests (${{ matrix.package }})
    needs: setup
    runs-on: ubuntu-latest
    # A backstop, not a budget. Most suites finish in single-digit minutes; if a
    # live test ever escapes its exclusion it blocks on a network call, and the
    # default 6h job timeout would let it sit there. Raised from 20 for
    # raxol_agent, whose 2426 tests take ~6 minutes on an 8-core dev box on top
    # of compiling main raxol and the termbox2 NIF, so 20 left no headroom on a
    # two-core runner.
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        # raxol_payments and raxol_earn move real money and were the two least
        # gated packages in the repo: the Sma7702Wallet alias bug (#858) shipped
        # with a compiler warning nobody could fail on, because nothing here ran
        # them. Adding them required unbreaking both suites first -- see the PR.
        #
        # Formatting is NOT what this matrix is for any more. Every package is
        # format-checked by the `format` job, which needs no compile, and the
        # root .formatter.exs delegates `packages/*` by glob -- so a package
        # joins here to get its TESTS run, not to earn a formatter entry.
        #
        # raxol_console boots agent packages onto the gateway and was ungated for
        # the same reason the others were: nothing in per-PR CI ran it. It joins
        # already clean, so this adds a gate rather than fixing drift.
        #
        # raxol_core and raxol_agent hold the containment boundaries and were
        # gated by nothing. Raxol.Core.Boundary.Path is the centralized
        # path-traversal confinement for the agent's fs tools; its 18 accept and
        # reject vectors live in packages/raxol_core/test/support/boundary_vectors/
        # and are copied verbatim into raxol_agent_client_protocol, which is the
        # only thing that makes the module's own claim -- "drift is a red test,
        # not a silent fork" -- true. Only the ACP copy had a red test available.
        # A one-sided corpus is a fork with extra steps.
        #
        # The same hole covers the process-kill work now landing in raxol_core
        # (killing an escaped port's whole process group by negative pgid): a
        # wrong target there takes the VM or the host down, and its tests would
        # have shipped unrun. raxol_agent is the package that consumes both, so
        # both join together.
        #
        # Keep on ONE line: test/raxol/formatter_delegation_test.exs parses this
        # matrix with `^\s*package: \[...\]` to prove every gated package is also
        # delegated by the root .formatter.exs. A block sequence parses as absent.
        package: [raxol_core, raxol_agent, raxol_mcp, raxol_gateway, raxol_telegram, raxol_cli, raxol_payments, raxol_earn, raxol_agent_client_protocol, raxol_console, raxol_symphony]

    steps:
      - uses: actions/checkout@v7

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict

      # Keyed on every mix.exs, not only this package's mix.lock.
      #
      # The cached path includes `_build`, which holds COMPILED sibling raxol
      # packages resolved through path deps. A version bump changes no
      # lockfile, so the key matched exactly, the cache restored 2.6.x sibling
      # .app files, and `mix deps.get --check-locked` refused them against the
      # new `~> 2.7` requirements: `the dependency does not match the
      # requirement "~> 2.7", got "2.6.0"`, in eight of these cells at once.
      # Path deps are not in a lockfile at all, so mix.lock cannot see them.
      #
      # The restore-keys prefix is deliberately gone as well: falling back to a
      # stale-versioned `_build` is precisely the failure being fixed, and a
      # cold compile is cheaper than a red matrix nobody can explain.
      - name: Cache package dependencies
        uses: actions/cache@v6
        with:
          path: |
            packages/${{ matrix.package }}/deps
            packages/${{ matrix.package }}/_build
          key: pkg-${{ matrix.package }}-${{ runner.os }}-${{ needs.setup.outputs.elixir-version }}-${{ needs.setup.outputs.otp-version }}-${{ hashFiles(format('packages/{0}/mix.lock', matrix.package)) }}-${{ hashFiles('mix.exs', 'packages/*/mix.exs') }}

      - name: Install hex and rebar
        run: |
          mix local.hex --force
          mix local.rebar --force

      # raxol_symphony's WorkflowStore watches WORKFLOW.md through `file_system`,
      # whose Linux backend shells out to `inotifywait`. Without it the backend
      # declines to start and the two watcher tests self-exclude, so the hot
      # reload path would be gated by a cell that never exercises it. Only this
      # package needs it, so only this package pays for it.
      - name: Install inotify-tools
        if: matrix.package == 'raxol_symphony'
        run: sudo apt-get update && sudo apt-get install -y inotify-tools

      # `--check-locked` rather than a bare `deps.get`: a plain fetch REWRITES
      # a stale mix.lock in place and carries on, so a package whose committed
      # lock does not match what resolution produces builds green in CI and
      # cannot be reproduced anywhere else. raxol_symphony and raxol_telegram
      # both drifted that way undetected (symphony would not even compile from
      # its committed lock on a clean checkout). `Lockstep Deps` does not cover
      # this -- it checks sibling "~> X.Y" constraints, not lock contents.
      # Fix a failure here by running `mix deps.get` in that package and
      # committing the lock.
      - name: Compile package
        working-directory: packages/${{ matrix.package }}
        run: |
          mix deps.get --check-locked
          mix compile --warnings-as-errors

      # The exclusions are stated here rather than left to each package's
      # test_helper. raxol_payments computes its list from env-var PRESENCE, so a
      # stray XOCHI_LIVE_URL anywhere in the workflow env drops :live_xochi and
      # the live suite -- which compile-gates on the same one variable -- starts
      # issuing requests on every PR, forks included. ExUnit unions these with
      # the helper's list, so nothing here can un-exclude a tag.
      #
      # :skip_on_ci is the repo-wide "this needs a real workstation" marker, and
      # the root suite honours it whenever SKIP_TERMBOX2_TESTS is set -- which is
      # workflow-global here. raxol_agent's test_helper never wired it up, so its
      # one tagged test (a chmod-0500 write-failure fault in the journal Writer)
      # was inert. Excluding it here applies the convention rather than inventing
      # a package-local one. No other gated package uses the tag.
      - name: Run package tests
        working-directory: packages/${{ matrix.package }}
        run: |
          mix test \
            --exclude live_xochi \
            --exclude live_xochi_matrix \
            --exclude live_xochi_preflight \
            --exclude live_xochi_order \
            --exclude live_xochi_order_preflight \
            --exclude live_xochi_order_settle \
            --exclude live_relay \
            --exclude live_property \
            --exclude live_parity \
            --exclude live_solver_fee \
            --exclude live_chain \
            --exclude live_bundler \
            --exclude live_acp_dev \
            --exclude cli_signer \
            --exclude skip_on_ci

      # The loopback is excluded from the default run (`:integration`, see
      # packages/raxol_agent/test/test_helper.exs) because it boots a real BEAM
      # peer over stdio, so without this step the ONE test that drives
      # `bin/raxol-acp` the way an editor does never ran in CI. It is included
      # by file rather than by tag: the other `:integration` suites in this
      # package need a live Postgres.
      #
      # Same reasoning as the input-protocol canary below. Everything this
      # catches is invisible to unit tests, because a unit test supplies the
      # frames instead of a process: non-JSON leaking onto the protocol pipe,
      # the shim's compile step swallowing the client's first frame, and a turn
      # that reports a stop reason while delivering no content. All three were
      # real, all three shipped, and none of them failed a suite.
      - name: Run ACP stdio loopback (real peer)
        if: matrix.package == 'raxol_agent'
        working-directory: packages/raxol_agent
        run: mix test --include integration test/raxol/agent/acp_loopback_test.exs

      # Same hole, other entrypoints: `bin/raxol` and `bin/raxol-code` are
      # shell scripts, so nothing in the Elixir suite goes through them -- unit
      # tests call `Raxol.Agent.P.run/1` directly and never see the process
      # contract the scripts own. Every defect these two shipped lived there:
      # Mix's loadpaths pass printing "==> raxol_terminal" / "Compiling N
      # files" onto the answer channel, and `app.start`'s log banner doing the
      # same. `:integration` for the BEAM boot, so included by file for the
      # same reason as the loopback above.
      - name: Run CLI entrypoint smoke (bin/raxol, bin/raxol-code)
        if: matrix.package == 'raxol_agent'
        working-directory: packages/raxol_agent
        run: mix test --include integration test/raxol/agent/cli_entrypoint_smoke_test.exs

      # Kept as a per-package backstop, and kept LAST. The `format` job checks
      # every package now, so this is redundant on the happy path -- but this
      # job is named "Package Tests" and two of its cells move real money, so a
      # red X here has to mean the tests ran, not that a line was two columns
      # too wide before compile started.
      - name: Check formatting
        working-directory: packages/${{ matrix.package }}
        run: mix format --check-formatted

  # The local terminal input path is BEAM/OTP-coupled: keystrokes arrive by
  # tracing prim_tty's reader (a private protocol). Every unit test fabricates
  # that trace message, so none can catch an OTP change to it. This canary types
  # real keystrokes into a real pty and asserts they decode, so the PR that bumps
  # the OTP pin in `mise.toml` fails here, loudly and per-PR, instead of on the
  # nightly matrix at 2am. Sub-second decode; the pty boot is the only cost.
  input-canary:
    name: Input protocol canary
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install pty drivers + NIF build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y tmux expect build-essential autoconf m4 \
            libncurses5-dev libssl-dev

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict

      - name: Install hex and rebar
        run: |
          mix local.hex --force
          mix local.rebar --force

      # Same reason as the package matrix above: `_build` holds compiled
      # sibling raxol packages, and a version bump changes no lockfile.
      - name: Cache raxol_terminal deps
        uses: actions/cache@v6
        with:
          path: |
            packages/raxol_terminal/deps
            packages/raxol_terminal/_build
          key: pkg-raxol_terminal-canary-${{ runner.os }}-${{ needs.setup.outputs.elixir-version }}-${{ needs.setup.outputs.otp-version }}-${{ hashFiles('packages/raxol_terminal/mix.lock') }}-${{ hashFiles('mix.exs', 'packages/*/mix.exs') }}

      - name: Run input protocol canary
        working-directory: packages/raxol_terminal
        env:
          MIX_ENV: test
          TMPDIR: /tmp
        run: |
          mix deps.get
          mix test test/raxol/terminal/input_protocol_canary_test.exs \
            --only input_protocol_canary

  test-integration:
    name: Integration Tests
    needs: [setup, compile]
    runs-on: ubuntu-latest
    if: needs.setup.outputs.should-run-heavy == 'true'
    
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: raxol_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}
      
      # Self-heal a cache that disagrees with the checked-out lock; see the
      # note on setup's deps.get fallback. No-op when the cache is healthy.
      - name: Fetch dependencies
        run: mix deps.get

      - name: Run integration tests
        run: |
          mix test --only integration --exclude skip_on_ci
        env:
          POSTGRES_HOST: localhost
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: raxol_test
          MIX_TEST_PARTITION: 2
      
      - name: Upload coverage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-integration
          path: cover/integration.coverdata

  test-property:
    name: Property Tests
    needs: [setup, compile]
    runs-on: ubuntu-latest
    if: needs.setup.outputs.should-run-heavy == 'true'
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}
      
      # Self-heal a cache that disagrees with the checked-out lock; see the
      # note on setup's deps.get fallback. No-op when the cache is healthy.
      - name: Fetch dependencies
        run: mix deps.get

      - name: Run property tests
        run: |
          # Run test/property by path, but still honor skip_on_ci: some
          # properties are wall-clock timing assertions (e.g. parser
          # sub-quadratic scaling) tagged skip_on_ci precisely because they
          # flake on shared CI runners. By path alone they would run and
          # flake; exclude them like every other CI lane.
          mix test test/property --exclude skip_on_ci
        env:
          MIX_TEST_PARTITION: 3
      
      - name: Upload coverage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-property
          path: cover/property.coverdata

  # ========================================
  # Code Quality Checks
  # ========================================
  # Advisory ONLY. nightly.yml's `credo-ratchet` job is what actually fails on
  # a regression, against priv/quality_baseline.json.
  #
  # `credo diff`, not `credo --strict`. A repo-wide pass takes ~50 min and
  # cannot be sharded by path (`mix credo <path>` analyzes nothing against the
  # root .credo.exs), so running one here spent most of an hour per PR on a
  # result nothing reads and that nobody waits for -- which is not feedback,
  # it is a queue. `diff` reports only what this branch introduced against its
  # merge base, which is both the fast answer and the only one an author can
  # act on: the other 1,182 findings are not theirs.
  credo:
    name: Credo Analysis (advisory)
    needs: [setup, compile]
    runs-on: ubuntu-latest
    continue-on-error: true
    timeout-minutes: 20

    steps:
      # `credo diff` resolves a merge base, which needs real history; the
      # default shallow clone has none and the command cannot run.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}
      
      # Self-heal a cache that disagrees with the checked-out lock; see the
      # note on setup's deps.get fallback. No-op when the cache is healthy.
      - name: Fetch dependencies
        run: mix deps.get

      # No `|| true`: `continue-on-error` above already makes this advisory,
      # and stacking both hid the count from the job's own exit status too.
      #
      # Against the merge base with the PR's own base branch, not a hardcoded
      # master: this repo stacks PRs (a branch's base is often another branch),
      # and diffing a stacked branch against master would report the parent
      # PR's findings as this one's.
      - name: Run Credo (new findings only)
        run: |
          base="${{ github.event.pull_request.base.ref || 'master' }}"
          mix credo diff --strict --from-git-merge-base "origin/${base}"

  dialyzer:
    name: Dialyzer (advisory)
    needs: [setup, compile]
    runs-on: ubuntu-latest
    continue-on-error: true
    if: needs.setup.outputs.should-run-heavy == 'true'
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}
      
      # Self-heal a cache that disagrees with the checked-out lock; see the
      # note on setup's deps.get fallback. No-op when the cache is healthy.
      - name: Fetch dependencies
        run: mix deps.get

      # The exact key includes the packages/ sources so a matching hit means
      # the PLT was built against exactly this code. dialyxir keys its own
      # deps PLT off the DEPS hash, which a path dependency does not move:
      # editing a callback spec in packages/raxol_core does not touch
      # mix.lock, so a mix.lock-only key restores a PLT describing the OLD
      # spec and dialyzer reports against stale types. Observed locally -- a
      # fix removing 16 callback_type_mismatch errors read as a no-op across
      # two full runs.
      #
      # `restore-keys` still allows a looser hit, which is deliberate: a
      # partial restore saves the PLT build for unrelated dependencies, and
      # check-quality-ratchet.sh detects the mismatch by CONTENT HASH and
      # rebuilds. An earlier revision detected it by mtime, which is wrong
      # here -- actions/checkout writes sources with mtime=now while
      # actions/cache restores the PLT with its archived mtime, so the check
      # fired on every warm-cache run and turned this blocking gate into an
      # unconditional failure.
      - name: Restore PLT cache
        uses: actions/cache@v6
        with:
          path: priv/plts
          key: plt-${{ runner.os }}-${{ needs.setup.outputs.otp-version }}-${{ needs.setup.outputs.elixir-version }}-${{ hashFiles('**/mix.lock') }}-${{ hashFiles('packages/*/lib/**/*.ex') }}
          restore-keys: |
            plt-${{ runner.os }}-${{ needs.setup.outputs.otp-version }}-${{ needs.setup.outputs.elixir-version }}-${{ hashFiles('**/mix.lock') }}-
            plt-${{ runner.os }}-${{ needs.setup.outputs.otp-version }}-${{ needs.setup.outputs.elixir-version }}-

      # No separate `mix dialyzer --plt` step: that call produces a deps PLT
      # the size of core.plt with none of the dependency apps in it, and
      # dialyxir then reports "PLT is up to date!" permanently because it
      # keys freshness off a 20-byte .plt.hash. Analysing against it yields a
      # fabricated regression (observed: 2055 findings, 1845 of them
      # `unknown_function`, against a true 97). The ratchet discards a stale
      # deps PLT and lets the analysis run rebuild it correctly, and refuses
      # to report a count when `unknown_function` dominates.
      #
      # Advisory, and nightly.yml's ratchet is what fails on a regression --
      # the same split this workflow already applies to credo, for a related
      # reason.
      #
      # The count is not reproducible across environments yet, so it cannot be
      # a merge gate. A warm local PLT reports 91 (matching the baseline) while
      # CI reported 100 on the same tree, and the cause is upstream of the
      # ratchet: the cache key above includes
      # `hashFiles('packages/*/lib/**/*.ex')`, so any PR touching packages/ --
      # most of them -- misses the exact key, restores through restore-keys,
      # and then has its deps PLT discarded as stale. Cold and warm PLTs
      # disagree by 9 findings, and blocking a merge on which one CI happened
      # to get is a gate that fails for environmental reasons rather than code
      # reasons. That is how a gate gets routinely overridden, which is worse
      # than an advisory one nobody can dismiss silently.
      #
      # Nightly runs the identical ratchet with no PR to block and time to
      # build a cold PLT properly, which is where the number means something.
      # Make this blocking again once a PR-time run reproduces the nightly
      # count -- prebuilding the PLT on a schedule is the obvious way.
      - name: Dialyzer ratchet
        run: ./scripts/check-quality-ratchet.sh --gate dialyzer
        timeout-minutes: 90

  # ========================================
  # Security Scanning
  # ========================================
  security:
    name: Security Scan
    needs: setup
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Restore cache
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
            .raxol_cache
          key: ${{ needs.setup.outputs.cache-key }}
      
      # Self-heal a cache that disagrees with the checked-out lock; see the
      # note on setup's deps.get fallback. No-op when the cache is healthy.
      - name: Fetch dependencies
        run: mix deps.get

      - name: Check for vulnerable dependencies
        run: |
          mix deps.audit || true
      
      - name: Run Sobelow security check
        run: |
          mix sobelow --config || true
        continue-on-error: true

  # ========================================
  # Cross-Platform Testing
  # ========================================
  test-cross-platform:
    name: Test ${{ matrix.os }}
    needs: [setup, compile]
    runs-on: ${{ matrix.os }}
    if: github.event_name == 'push' || github.event.pull_request.draft == false
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-2022]
        include:
          - os: macos-latest
            platform: macos
          - os: windows-2022
            platform: windows
    
    steps:
      - uses: actions/checkout@v7
      
      - name: Set TMPDIR (Unix)
        if: runner.os != 'Windows'
        run: echo "TMPDIR=/tmp" >> $GITHUB_ENV

      - name: Set TMPDIR (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          echo "TMPDIR=$env:TEMP" >> $env:GITHUB_ENV
          echo "SKIP_TERMBOX2_TESTS=true" >> $env:GITHUB_ENV

      - name: Install macOS dependencies
        if: matrix.os == 'macos-latest'
        run: |
          # Do NOT export LDFLAGS/CFLAGS here: a global LDFLAGS clobbers the
          # termbox2 NIF Makefile's `LDFLAGS ?= -shared`, dropping -shared so the
          # .so links as an executable (`_main` undefined). Nothing in the build
          # needs openssl flags; OTP ships precompiled.
          brew install postgresql@14

          # Start PostgreSQL
          brew services start postgresql@14 || true
          sleep 5
          createuser -s postgres || true
          psql -U postgres -c "ALTER USER postgres WITH PASSWORD 'postgres';" postgres || true
          createdb -U postgres raxol_test || true
      
      - name: Install Windows dependencies
        if: matrix.os == 'windows-latest'
        run: |
          choco install openssl postgresql14 --no-progress
          echo "POSTGRES_HOST=localhost" >> $env:GITHUB_ENV
          echo "POSTGRES_PORT=5432" >> $env:GITHUB_ENV
      
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ needs.setup.outputs.elixir-version }}
          otp-version: ${{ needs.setup.outputs.otp-version }}
          version-type: strict
      
      - name: Cache dependencies
        uses: actions/cache@v6
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ needs.setup.outputs.otp-version }}-${{ needs.setup.outputs.elixir-version }}-${{ hashFiles('**/mix.exs') }}-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-${{ needs.setup.outputs.otp-version }}-${{ needs.setup.outputs.elixir-version }}-${{ hashFiles('**/mix.exs') }}-
      
      - name: Install dependencies
        # Force bash (git-bash on Windows). The default Windows shell is pwsh,
        # where `mix deps.get --check-locked || mix deps.get` does not reliably
        # run the fallback for mix.bat, so upstream lock drift is never resolved
        # and `mix test` then fails with a stale-dep "lock mismatch". bash runs
        # the fallback identically on every OS.
        shell: bash
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.clean --unused
          # Prefer the committed lock (fast-fails if mix.exs was edited without
          # a committed mix.lock). Fall back to a plain resolve so pure upstream
          # drift -- a transitive dep publishing a newer patch after the lock was
          # committed -- does not red the whole pipeline. The pre-commit hook and
          # `mix raxol.check` still guard genuinely uncommitted lock changes.
          mix deps.get --check-locked || mix deps.get
          mix deps.compile

      - name: Run tests (Unix)
        if: runner.os != 'Windows'
        run: mix test --exclude slow --exclude docker --exclude integration --exclude skip_on_ci
        env:
          MIX_ENV: test
          SKIP_TERMBOX2_TESTS: "true"
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: raxol_test
          PLATFORM: ${{ matrix.platform }}

      - name: Run tests (Windows)
        if: runner.os == 'Windows'
        # --timeout 180000: the Windows runner is CPU-starved enough that
        # emulator-replay-heavy property tests (substrate keystones, SSH
        # rendering round-trips) blow the 60s default as a runner-speed
        # artifact — a different one each run. Raising the job-wide ceiling
        # keeps full coverage; genuine hangs still fail at the raised bound.
        run: mix test --timeout 180000 --exclude slow --exclude docker --exclude integration --exclude skip_on_ci --exclude skip_on_windows
        env:
          MIX_ENV: test
          SKIP_TERMBOX2_TESTS: "true"
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: raxol_test
          PLATFORM: ${{ matrix.platform }}

  # ========================================
  # Coverage Report
  # ========================================
  coverage:
    name: Coverage Report
    needs: [test-unit, test-integration, test-property]
    runs-on: ubuntu-latest
    # Temporarily disabled due to ExCoveralls crash
    if: false
    
    steps:
      - uses: actions/checkout@v7
      
      # Reads the pin directly rather than through `needs.setup`, which this job
      # does not depend on: those outputs resolve to empty here.
      - name: Set up Elixir
        uses: erlef/setup-beam@v1
        with:
          version-file: mise.toml
          version-type: strict

      - name: Download coverage artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: cover
      
      - name: Merge coverage reports
        run: |
          mix coveralls.json --import-cover cover/**/*.coverdata
      
      - name: Upload to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./cover/excoveralls.json
          flags: unittests
          name: raxol-coverage
          fail_ci_if_error: false
      
      - name: Comment coverage on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v9
        with:
          script: |
            const fs = require('fs');
            const coverage = JSON.parse(fs.readFileSync('./cover/excoveralls.json', 'utf8'));
            const percentage = coverage.percentage;
            
            const comment = `## 📊 Coverage Report
            
            **Total Coverage:** ${percentage}%
            
            | Type | Coverage |
            |------|----------|
            | Unit Tests | ✅ |
            | Integration Tests | ${{ needs.test-integration.result == 'success' && '✅' || '⏭️' }} |
            | Property Tests | ${{ needs.test-property.result == 'success' && '✅' || '⏭️' }} |
            `;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

  # ========================================
  # Final Status Check
  # ========================================
  ci-status:
    name: CI Status
    # Neither `credo` nor `dialyzer` is listed: both are advisory (see their
    # job comments) and both are enforced by nightly.yml's ratchet jobs.
    #
    # Listing a job here without reading its `result` is the trap this comment
    # exists to prevent -- `credo` was declared and never read, so it looked
    # gated and was not. If either becomes blocking again, add it to `needs`
    # AND add a case that reads its result.
    needs:
      [
        format,
        release-check,
        compile,
        test-unit,
        package-tests,
        input-canary,
        security
      ]
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Check status
        run: |
          # Required checks
          if [ "${{ needs.format.result }}" != "success" ]; then
            echo "❌ Format check failed"
            exit 1
          fi

          if [ "${{ needs.release-check.result }}" != "success" ]; then
            echo "Package release check failed"
            exit 1
          fi

          if [ "${{ needs.compile.result }}" != "success" ]; then
            echo "❌ Compilation check failed"
            exit 1
          fi

          if [ "${{ needs.test-unit.result }}" != "success" ]; then
            echo "❌ Unit tests failed"
            exit 1
          fi

          if [ "${{ needs.package-tests.result }}" != "success" ]; then
            echo "❌ Package tests failed"
            exit 1
          fi

          if [ "${{ needs.input-canary.result }}" != "success" ]; then
            echo "❌ Input protocol canary failed"
            exit 1
          fi

          # Dialyzer is deliberately absent: it is advisory until a PR-time run
          # reproduces the nightly count (see its job comment). nightly.yml's
          # `dialyzer` ratchet is what fails on a regression.

          if [ "${{ needs.security.result }}" != "success" ]; then
            echo "⚠️ Security scan found issues (non-blocking)"
          fi
          
          echo "✅ All required checks passed!"