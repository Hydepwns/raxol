import Config

config :raxol,
  terminal:
    %{
      # Missing required fields
    },
  buffer:
    %{
      # Missing required fields
    },
  renderer:
    %{
      # Missing required fields
    }
